apiVersion: v1
kind: Service
metadata:
  name: {{ include "telegram-channel.fullname" . }}
  labels:
    {{- include "telegram-channel.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "telegram-channel.selectorLabels" . | nindent 4 }}
