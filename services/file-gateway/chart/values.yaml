fullnameOverride: "file-gateway"

global:
  annotations:
    ark.mckinsey.com/service: file-gateway

storage:
  enabled: true
  size: 1Gi
  storageClassName: ""
  accessModes:
    - ReadWriteOnce
  pvcName: ""

httpRoute:
  enabled: true
  annotations: {}
  parentRefs:
    - name: localhost-gateway
      namespace: ark-system
  hostnames:
    - "file-gateway.127.0.0.1.nip.io"
    - "file-gateway.default.127.0.0.1.nip.io"

versitygw:
  enabled: true
  replicaCount: 1

  # Storage backend for the gateway:
  #   posix - store objects on the shared PVC at config.dataPath (default)
  #   s3    - proxy to an existing S3 service (e.g. real AWS S3). VersityGW mounts
  #           no PVC in this mode; also set storage.enabled=false to skip
  #           provisioning the (otherwise unused) PVC. Requires filesystemMcp.enabled=false.
  backend: posix

  image:
    repository: ghcr.io/versity/versitygw
    pullPolicy: IfNotPresent
    tag: "latest"

  serviceAccount:
    create: true
    name: ""

  podSecurityContext:
    runAsNonRoot: true
    runAsUser: 1000
    runAsGroup: 1000
    fsGroup: 1000

  securityContext:
    allowPrivilegeEscalation: false
    capabilities:
      drop:
        - ALL

  service:
    type: ClusterIP
    port: 80

  resources:
    limits:
      cpu: 1000m
      memory: 1Gi
    requests:
      cpu: 500m
      memory: 512Mi

  config:
    region: "us-east-1"
    port: "7070"
    dataPath: "/data"

  # Used only when backend: s3 - upstream S3 service the gateway proxies to.
  s3Backend:
    # Upstream S3 endpoint. Empty = AWS default (https://s3.<region>.amazonaws.com).
    endpoint: ""
    region: "us-east-1"
    # Optional secret holding upstream credentials as keys:
    #   AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, and optionally AWS_SESSION_TOKEN.
    # Leave empty to use the pod's AWS default credential chain (IRSA / pod identity),
    # but you must then also set allowDefaultCredentialChain=true (see below).
    credentialsSecret: ""
    # Guard: when credentialsSecret is empty, install fails unless this is true.
    # Prevents silently deploying with no upstream credentials (pod reports Ready
    # but every S3 request 403s). Set true only when using IRSA / pod identity.
    allowDefaultCredentialChain: false

  s3SecretName: "file-gateway-s3-credentials"

  createSecret: true
  s3Credentials:
    accessKeyId: "admin"
    secretAccessKey: "change-me"

filesystemMcp:
  enabled: true
  replicaCount: 1

  image:
    repository: ghcr.io/mckinsey/agents-at-scale-ark/filesystem-mcp-server
    pullPolicy: IfNotPresent
    tag: "latest"

  serviceAccount:
    create: true
    name: ""

  podSecurityContext:
    runAsNonRoot: true
    runAsUser: 1000
    fsGroup: 1000

  securityContext:
    allowPrivilegeEscalation: false
    capabilities:
      drop:
        - ALL

  service:
    type: ClusterIP
    port: 80

  resources:
    limits:
      cpu: 500m
      memory: 512Mi
    requests:
      cpu: 250m
      memory: 256Mi

  config:
    logLevel: INFO
    host: "0.0.0.0"
    port: "8000"
    dataPath: "/data"
    bucketName: "aas-files"

  # When set, MCPServer metadata.name is this value so ARK discovers tools under this name
  # (e.g. "mcp-filesystem" so agents that expect mcp-filesystem-* tools work without standalone MCP).
  mcpserverName: ""

fileApi:
  enabled: true
  replicaCount: 1

  image:
    repository: ghcr.io/mckinsey/agents-at-scale-marketplace/file-api
    pullPolicy: IfNotPresent
    tag: "latest"

  serviceAccount:
    create: true
    name: ""

  podSecurityContext:
    runAsNonRoot: true
    runAsUser: 1000
    fsGroup: 1000

  securityContext:
    allowPrivilegeEscalation: false
    capabilities:
      drop:
        - ALL

  service:
    type: ClusterIP
    port: 80

  resources:
    limits:
      cpu: 500m
      memory: 512Mi
    requests:
      cpu: 250m
      memory: 256Mi

  config:
    host: "0.0.0.0"
    port: "8080"
    # S3 bucket file-api serves. Empty = fall back to filesystemMcp.config.bucketName.
    # IMPORTANT: when versitygw.backend=s3 this MUST name an existing upstream bucket
    # (nothing in the chart auto-creates it); the default "aas-files" will not exist
    # in a real S3 account and requests will fail with NoSuchBucket.
    bucketName: ""

  cors:
    origins: "http://localhost:3000"
    allowCredentials: true
    allowMethods: "*"
    allowHeaders: "*"
