File Gateway has been installed!

Resources:
{{- if .Values.versitygw.enabled }}
- VersityGW S3 API: {{ include "file-gateway.fullname" . }}-versitygw:{{ .Values.versitygw.service.port }}
{{- end }}
{{- if .Values.filesystemMcp.enabled }}
- Filesystem MCP: {{ include "file-gateway.fullname" . }}-filesystem-mcp:{{ .Values.filesystemMcp.service.port }}
{{- end }}
{{- if .Values.fileApi.enabled }}
- File API: {{ include "file-gateway.fullname" . }}-api:{{ .Values.fileApi.service.port }}
{{- end }}
{{- if and .Values.storage.enabled (ne .Values.versitygw.backend "s3") }}
- Shared Storage (PVC): {{ include "file-gateway.pvcName" . }} ({{ .Values.storage.size }})
{{- end }}

Storage backend: {{ .Values.versitygw.backend }}
{{- if eq .Values.versitygw.backend "s3" }}
  VersityGW proxies the remote bucket {{ .Values.fileApi.config.bucketName | default .Values.filesystemMcp.config.bucketName | quote }}; no PVC is provisioned.
{{- else }}
  Objects are stored on the shared PVC at {{ include "file-gateway.filesystemMcp.dataPath" . }}.
{{- end }}

To verify the deployment:

  kubectl get pods -l app.kubernetes.io/name={{ include "file-gateway.name" . }}
  kubectl get svc -l app.kubernetes.io/name={{ include "file-gateway.name" . }}
{{- if and .Values.storage.enabled (ne .Values.versitygw.backend "s3") }}
  kubectl get pvc {{ include "file-gateway.pvcName" . }}
{{- end }}
{{- if .Values.fileApi.enabled }}

To test the File API:

  kubectl port-forward svc/{{ include "file-gateway.fullname" . }}-api 8080:{{ .Values.fileApi.service.port }}
  curl http://localhost:8080/health
  curl http://localhost:8080/files
{{- end }}
{{- if .Values.filesystemMcp.enabled }}

To check the Filesystem MCP health (image has no shell tools, so port-forward from the host):

  kubectl port-forward svc/{{ include "file-gateway.fullname" . }}-filesystem-mcp 8000:{{ .Values.filesystemMcp.service.port }}
  curl http://localhost:8000/health
{{- end }}

Configuration:
- Bucket: {{ .Values.fileApi.config.bucketName | default .Values.filesystemMcp.config.bucketName }}
- Gateway root credentials: stored in secret {{ include "file-gateway.s3SecretName" . }} (keys: access-key-id, secret-access-key)
{{- if eq .Values.versitygw.backend "s3" }}
- Upstream S3 credentials: {{ if .Values.versitygw.s3Backend.credentialsSecret }}stored in secret {{ .Values.versitygw.s3Backend.credentialsSecret }}{{ else }}AWS default credential chain (IRSA / pod identity){{ end }}
{{- end }}
