{{- if and .Values.filesystemMcp.enabled .Values.filesystemMcp.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "file-gateway.filesystemMcp.serviceAccountName" . }}
  labels:
    {{- include "file-gateway.labels" . | nindent 4 }}
    app.kubernetes.io/component: filesystem-mcp
{{- end }}
