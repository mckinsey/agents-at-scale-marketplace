MCP Inspector has been deployed!

{{- if .Values.httpRoute.enabled }}

Dashboard URL:
  http://mcp-inspector.{{ .Release.Namespace }}.127.0.0.1.nip.io:8080/?MCP_PROXY_FULL_ADDRESS=http://mcp-inspector-proxy.{{ .Release.Namespace }}.127.0.0.1.nip.io:8080
{{- end }}

Port forward:
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "mcp-inspector.fullname" . }} 6274:6274

Usage:
  1. Go to Configuration and set Proxy Session Token to 'test'
  2. Connect to an MCP server: kubectl get mcpserver -A
