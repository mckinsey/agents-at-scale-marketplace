apiVersion: v1
kind: Service
metadata:
  name: {{ include "a2a-inspector.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "a2a-inspector.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "a2a-inspector.selectorLabels" . | nindent 4 }}
