"""LiteLLM model provider — reconciles Ark Model resources and scoped LiteLLM
virtual keys into tenant namespaces, so tenants reference models without ever
holding provider credentials."""

__version__ = "0.1.0"
