# Runtime dependencies for the ark_evals engine.
# Deliberately lean: everything else (HTTP, multipart, JSON) is stdlib, and the
# judge is called via `kubectl` (present in the image), so no Kubernetes client.
pydantic>=2.0.0,<3.0.0
