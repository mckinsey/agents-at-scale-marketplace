version: v2beta1

pipelines:
  deploy: |-
    create_deployments --all
  dev: |-
    create_deployments --all

deployments:
  kyc-onboarding-bundle:
    namespace: kyc-onboarding-demo
    helm:
      chart:
        path: chart
      values: {}
