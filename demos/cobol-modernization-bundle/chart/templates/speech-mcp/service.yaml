{{- if .Values.speechMcp.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.speechMcp.name }}
  labels:
    category: cobol-modernization
    app.kubernetes.io/component: speech-mcp
    {{- with .Values.commonLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ .Values.speechMcp.name }}
    app.kubernetes.io/component: speech-mcp
{{- end }}
