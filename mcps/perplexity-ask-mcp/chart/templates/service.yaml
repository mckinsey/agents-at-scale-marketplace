apiVersion: v1
kind: Service
metadata:
  name: {{ include "perplexity-ask-mcp.fullname" . }}
  labels:
    {{- include "perplexity-ask-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "perplexity-ask-mcp.selectorLabels" . | nindent 4 }}
