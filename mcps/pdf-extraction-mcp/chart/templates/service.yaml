apiVersion: v1
kind: Service
metadata:
  name: {{ include "pdf-extraction-mcp.fullname" . }}
  labels:
    {{- include "pdf-extraction-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pdf-extraction-mcp.selectorLabels" . | nindent 4 }}
