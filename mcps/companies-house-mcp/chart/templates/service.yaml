apiVersion: v1
kind: Service
metadata:
  name: {{ include "companies-house-mcp.fullname" . }}
  labels:
    {{- include "companies-house-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "companies-house-mcp.selectorLabels" . | nindent 4 }}
