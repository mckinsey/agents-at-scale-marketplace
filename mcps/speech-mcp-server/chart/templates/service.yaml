apiVersion: v1
kind: Service
metadata:
  name: {{ include "speech-mcp.fullname" . }}
  labels:
    {{- include "speech-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "speech-mcp.selectorLabels" . | nindent 4 }}
