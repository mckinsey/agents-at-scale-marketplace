apiVersion: v1
kind: Service
metadata:
  name: {{ include "web-research-mcp.fullname" . }}
  labels:
    {{- include "web-research-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "web-research-mcp.selectorLabels" . | nindent 4 }}
